# Database URL
# PostgreSQL (recommended for production):
DATABASE_URL=postgresql://user:password@localhost:5432/bounties

# SQLite (for local development):
# DATABASE_URL=sqlite:///./bounties.db

# Server settings
HOST=0.0.0.0
PORT=8000
